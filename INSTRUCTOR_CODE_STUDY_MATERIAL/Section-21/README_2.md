The difference between the two Docker Compose files is that one uses the Kafka Docker image from Bitnami, while the other uses the Kafka Docker image from Confluent.

Since Bitnami has recently announced that they will be removing their Kafka Docker image from Docker Hub, it is preferable to use the Confluent image.